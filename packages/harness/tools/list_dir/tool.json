{
  "name": "list_dir",
  "description": "List files and directories within your entity home. Pass an empty string or '.' to list the root. Returns array of {name, type} entries.",
  "version": "1.0.0",
  "invocation": "native",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Relative path within your entity directory. Use '' or '.' for root."
      }
    },
    "required": []
  }
}
